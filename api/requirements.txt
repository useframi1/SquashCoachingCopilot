# FastAPI and web server (API-specific dependencies)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database (API-specific dependencies)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Data validation (API-specific dependencies)
pydantic==2.5.0

# Additional dependencies that may not be in wheel metadata
# Use >= to allow versions from wheels to take precedence
xgboost>=2.0.0
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Note: Local packages are installed BEFORE this requirements.txt
# Their dependencies will be installed with the versions they were built with
# The above dependencies will only install if missing or upgrade if needed
